#!/usr/bin/env python3
"""Run sleeper-gini directly from the repo."""

import sys
from pathlib import Path

# Add src to path
sys.path.insert(0, str(Path(__file__).parent / "src"))

from sleeper_gini.cli import main

sys.exit(main())
